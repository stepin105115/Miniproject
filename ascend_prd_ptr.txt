#include <stdio.h>
#include <stdlib.h>
void order(int *a,int l){
    int t;
    for(int i=0;i < l;i++){
        for(int j = i+1;j< l;j++){
            if(a[i]> a[j]){
                t = a[i];
                a[i] = a[j];
                a[j] = t;
            }
        }
        printf("%d ",a[i]);
    }
}
int main() {
	int a[] = {5,2,8,1,4};
	int *p = a;
	order(a,5);
	return 0;
}